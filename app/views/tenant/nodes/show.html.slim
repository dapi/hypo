h1.mb-5 Узел #{node.title}

p Создан #{l node.created_at}
p Статус #{node_state node.state}
p Адрес #{link_to node.url, node.url}

.mt-5
  = link_to 'Удалить', tenant_node_path(node), data: { turbo_method: :delete }, class: 'btn btn-outline-danger'
